
const u = require("../../index");

const awsScope = require("../../library/awsScope.js");
const index = require("../../index.js");

u.scope(__filename, x => {
    // TODO: Fix broken test
    //u.assert(false);
});
